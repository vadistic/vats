query HoverCardCandidateQuery($id: ID!) {
  candidate(where: { id: $id }) {
    id
    id
    firstName
    lastName
    metaCompany
    metaHeadline
    emails
    phones
    links
    avatar {
      url
      name
    }
  }
}
