endpoint: ${env:PRISMA_ENDPOINT}/${env:PRISMA_SERVICE}/${env:PRISMA_STAGE}

datamodel:
  - datamodel.graphql

# seed:
#   import: seed.graphql

hooks:
  post-deploy:
    - echo "Deployment finished"
    - graphql get-schema --project database
    - graphql-override
    - graphql codegen
    - node scripts/fixTypings.js

secret: ${env:PRISMA_SECRET}
