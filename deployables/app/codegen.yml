overwrite: true
schema: '../server/src/generated/server.graphql'
documents: null
generates:
  # TODO: somehow load from ts
  # (cannot get require & loaders to work...)
  src/generated/resolvers.ts:
    schema: 'src/generated/client.graphql'
    config:
      noNamespaces: true
    plugins:
      - 'typescript-common'
      - 'typescript-server'
      - 'typescript-resolvers'
