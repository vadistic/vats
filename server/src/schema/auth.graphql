input SignupInput {
  password: String!
  username: String!
  inviteId: ID!
}

input LoginInput {
  email: String!
  password: String!
}

type AuthPayload {
  token: String!
  user: User!
}

type Mutation {
  signup(data: SignupInput): AuthPayload!
  login(data: LoginInput): AuthPayload!
}
